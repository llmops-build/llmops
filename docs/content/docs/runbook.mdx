---
title: AI Integration Runbook
description: Step-by-step guide for AI agents to integrate LLMOps into a project.
---

# LLMOps Integration Runbook

This is a structured guide for AI agents to integrate LLMOps into a TypeScript project.

## Prerequisites

Before starting, verify the following:

- [ ] Node.js 18+ is installed
- [ ] Project uses TypeScript
- [ ] Project uses one of the supported frameworks mentioned in https://llmops.build/llms.txt integrations list.
- [ ] An API key from at least one supported provider (OpenAI, Groq, Google, etc.)

## Step 1: Install Dependencies

Install the LLMOps SDK:

```bash
npm install @llmops/sdk
```

## Step 2: Create Environment Variables

Add provider API keys to `.env` file (create if it doesn't exist):

```bash
OPENAI_API_KEY=sk-...
GROQ_API_KEY=gsk-...
```

Make sure `.env` is in `.gitignore`.

## Step 3: Create LLMOps Configuration File

Create a new file `llmops.ts` (or `llmops.config.ts`) in the project root or `src/` directory:

```typescript
import { llmops } from '@llmops/sdk';

export default llmops({
  basePath: '/llmops',
});
```

Providers are auto-detected from environment variables. You can also declare them explicitly:

```typescript
import { llmops } from '@llmops/sdk';

export default llmops({
  basePath: '/llmops',
  providers: [
    {
      provider: 'openai',
      slug: 'openai',
      apiKey: process.env.OPENAI_API_KEY,
    },
  ],
});
```

## Step 4: Integrate Middleware

### For Express

Modify the main server file (e.g., `server.ts`, `app.ts`, or `index.ts`):

```typescript
import 'dotenv/config';
import express from 'express';
import { createLLMOpsMiddleware } from '@llmops/sdk/express';
import llmopsClient from './llmops'; // Adjust path as needed

const app = express();
const port = process.env.PORT || 3000;

app.use(express.json());

// Mount LLMOps middleware
app.use('/llmops', createLLMOpsMiddleware(llmopsClient));

// ... rest of your routes

app.listen(port, () => {
  console.log(`Server running at http://localhost:${port}`);
  console.log(`LLMOps API at http://localhost:${port}/llmops/api/genai/v1`);
});
```

### For Hono

Modify the main server file:

```typescript
import 'dotenv/config';
import { Hono } from 'hono';
import { serve } from '@hono/node-server';
import { createLLMOpsMiddleware } from '@llmops/sdk/hono';
import llmopsClient from './llmops'; // Adjust path as needed

const app = new Hono();
const port = process.env.PORT || 3000;

// Mount LLMOps middleware
app.use('/llmops/*', createLLMOpsMiddleware(llmopsClient));

// ... rest of your routes

serve({ fetch: app.fetch, port }, (info) => {
  console.log(`Server running at http://localhost:${info.port}`);
  console.log(`LLMOps API at http://localhost:${info.port}/llmops/api/genai/v1`);
});
```

## Step 5: Verify Installation

1. Start the server:

   ```bash
   npm run dev
   # or
   npx tsx server.ts
   ```

2. Test the health endpoint:

   ```bash
   curl http://localhost:3000/llmops/health
   ```

3. Make a test request through the gateway:

   ```bash
   curl -X POST http://localhost:3000/llmops/api/genai/v1/chat/completions \
     -H "Content-Type: application/json" \
     -d '{
       "model": "@openai/gpt-4o-mini",
       "messages": [{"role": "user", "content": "Hello!"}]
     }'
   ```

## Troubleshooting

### Module Not Found Errors

- Run `npm install` again
- Check import paths match file locations
- Verify `tsconfig.json` has proper module resolution

### Provider API Errors

- Verify the provider API key is valid and has credits
- Ensure the provider is OpenAI-compatible or has a dedicated adapter
- Check that the provider slug in the model string matches a configured provider

### Gateway Returns 400

- Verify the model uses the `@slug/model` format
- Check that the provider slug is configured in `llmops.ts` or auto-detected
- Divergent providers (Anthropic, Cohere, etc.) require a custom adapter via `config.adapters`

## File Structure After Integration

```
project/
├── .env                 # Environment variables (provider API keys)
├── llmops.ts           # LLMOps configuration
├── server.ts           # Server with LLMOps middleware
├── package.json        # Dependencies including @llmops/sdk
└── ...
```

## Summary

After completing these steps, you will have:

1. LLMOps SDK installed
2. OpenAI-compatible API available at `/llmops/api/genai/v1/`
3. Provider auto-detection from environment variables
4. Direct provider routing via `@slug/model` syntax
